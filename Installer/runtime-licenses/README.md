# Runtime Licenses

This directory is packaged as `LICENSES/` in `WAPL-Runtime-v*.zip`.

Before publishing a runtime bundle, include the license and notice files
distributed with each bundled Python, Node.js, Git, pnpm, and uv build.
